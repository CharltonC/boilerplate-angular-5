# Comment
language: node_js

node_js:
  - "6.9.4"

cache: npm

# Not required to install global dependency, e.g. angular-cli
install:
  - npm install

before_script:
  - cp -rf ./patch/ng-cli/ ./node_modules/

script:
  - cat ./node_modules/@angular/cli/models/webpack-configs/common.js

addons:
  chrome: stable